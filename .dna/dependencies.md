# 依赖声明

> 本文件是 AI 的依赖白名单和语义上下文。物理依赖（package.json / .csproj）由原生工具管理，此处聚焦于为什么依赖、边界契约是什么、允许使用哪些 API。

- 无（vena-core 是基础层，无任何外部程序集依赖）

## 说明

Vena Core 是依赖树的最底层：
- 不依赖任何游戏业务程序集
- 不依赖 `UnityEngine`（Math 模块使用自实现数学类型）

上层模块可单向依赖 Core，Core 永不反向引用。
