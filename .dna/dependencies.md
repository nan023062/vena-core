# 依赖声明

> 本文件是 AI 的依赖白名单和语义上下文。物理依赖（package.json / .csproj）由原生工具管理，此处聚焦于 **为什么依赖、边界契约是什么、允许使用哪些 API**。

- 无（底层包，无外部程序集依赖）

## 依赖列表

<!-- 格式：
- **程序集名**
  - 为什么依赖：解释业务/技术原因
  - 使用的 API：列出允许调用的公共接口
  - 边界契约：调用约束
  - [NOTICE]：（由依赖方 Breaking Change 时追加，处理后删除）
-->
